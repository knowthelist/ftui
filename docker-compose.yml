services:
  ftui:
    build: .
    # To use the pre-built image from Docker Hub instead of building locally:
    # image: knowthelist/ftui
    container_name: ftui
    ports:
      - "8080:80"
    volumes:
      - ./www/ftui/index.html:/usr/share/nginx/html/index.html:ro
    restart: unless-stopped